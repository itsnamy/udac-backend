spring.application.name=udac.backend

# Mail Settings
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=your-real-email@gmail.com
spring.mail.password=your-16-char-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# File upload settings
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Tomcat settings (correct ones)
server.tomcat.max-http-form-post-size=20971520
server.tomcat.max-swallow-size=50MB

# Optional: Connection timeout for slow uploads
server.tomcat.connection-timeout=30000

# Set the base directory to store uploaded videos
file.upload-dir=uploads/videos
